{
  "name": "clicker-game",
  "version": "1.0.0",
  "description": "A fun cookie clicker game with automated testing",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cd dist && git init && git add -A && git commit -m 'Deploy' && git push -f git@github.com:USERNAME/REPO.git main:gh-pages",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "vitest run --dir tests/unit",
    "test:integration": "vitest run --dir tests/integration",
    "test:watch": "vitest --dir tests/unit",
    "test:coverage": "vitest run --coverage --dir tests/unit",
    "test:ui": "vitest --ui"
  },
  "keywords": [
    "clicker",
    "game",
    "cookie"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vitest/ui": "^3.2.4",
    "happy-dom": "^20.0.0",
    "jsdom": "^27.0.0",
    "vite": "^7.1.9",
    "vitest": "^3.2.4"
  }
}
